package org.example;

public class Constructors {
    public static void main(String[] args) {
        // super() calls a constructor in its parent class
        // this() calls a constructor from its own class

        // constructors are not inherited
        // They are added implicitly if there aren't any explicitly stated in a child
        // this only works IF there is either no declared parent constructor or an explicit no-args constructor
    }
}
